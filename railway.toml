[build]
builder = "nixpacks"
build_command = """
python3 -m venv /opt/venv && \
. /opt/venv/bin/activate && \
pip install --upgrade pip && \
pip install -r requirements.txt
"""

[start]
start_command = "/opt/venv/bin/python -m uvicorn server:app --host 0.0.0.0 --port 8000"